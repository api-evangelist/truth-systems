generated: '2026-07-21'
method: derived
source: openapi/truth-systems-gateway-openapi-original.json
description: >-
  Entity graph for the Truth Systems Gateway API, derived from the OpenAPI
  component schemas and the SDK object model documented at
  docs.truthsystems.ai/sdk/ruling. The wire schemas (gatewayInput /
  gatewayOutput) map to the SDK's richer Ruling object model.
entities:
  - name: gatewayInput
    description: Request envelope - params with a claim and its sources
    fields: [params.claim, params.sources]
  - name: source
    description: A plain-text source document with an id (SDK TextSource)
    fields: [id, text]
  - name: gatewayOutput
    description: Response envelope - detailed_responses and sentences
    fields: [detailed_responses, sentences]
  - name: detailed_response
    description: Per-atomic-statement prediction with vector, span, and top-5 evidence sentences
    fields: [atomic, prediction, prediction_num, prediction_vector, span, top5_sentences]
  - name: sentence
    description: Per-sentence verdict with evidence ids, scores, and source spans
    fields: [evidence.ids, evidence.scores, evidence.source_spans, span, verdict]
  - name: Error
    description: Error envelope
    fields: [error, message]
relationships:
  - from: gatewayInput
    to: source
    type: has_many
    via: params.sources
  - from: gatewayOutput
    to: detailed_response
    type: has_many
    via: detailed_responses
  - from: gatewayOutput
    to: sentence
    type: has_many
    via: sentences
  - from: sentence
    to: source
    type: belongs_to
    via: evidence.ids
  - from: detailed_response
    to: source
    type: belongs_to
    via: top5_sentences (source ids)
sdk_model:
  docs: https://docs.truthsystems.ai/sdk/ruling
  types:
    - name: Ruling
      description: Result of client.judge() - overall verdict plus statements
    - name: Statement
      description: A single statement decomposed from the claim, with span, verdict, and influences
    - name: Influence
      description: Evidence from a source that influenced a verdict
    - name: TextInfluence
      description: Evidence from a TextSource, includes character spans
    - name: TextSource
      description: A plain-text source document
    - name: Verdict
      description: 'Enum: SUPPORTS, REFUTES, NOT_ENOUGH_INFO (overall verdict is conservative: REFUTES if any statement refuted, SUPPORTS only if all supported)'
